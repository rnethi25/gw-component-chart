{{- if .Values.customServiceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.customServiceAccount.name }}
  labels:
    {{- include "gw-component-chart.labels" . | nindent 4 }}
{{- end }}