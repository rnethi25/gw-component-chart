#### DevSpace Helm Chart ####
# This is a highly configurable Helm chart. For information on how to configure this file,
# take a look at the documentation: https://devspace.cloud/docs/cli/deployment/components/configuration/overview-specification

# A list of containers that should run the pods of this component
containers: []
initContainers: []

service:
  type: ClusterIP
  labels: {}
  annotations: {}
  ports: []
  # appProtocol: # Add this to each port definition
  externalIPs: []

labels: {}
annotations: {}

ingress:
  disabled: false
  labels: {}
  annotations: {}
  certmanager: true
  rules: []

# Define persistent volumes here
# Then mount them in containers above
volumes: []

# Defines image pull secrets
# DevSpace CLI will append all pull secrets which are automatically created
# More infos on: https://devspace.cloud/docs/cli/image-building/workflow-basics#8-create-image-pull-secret
pullSecrets: []

customServiceAccount:
  create: false

serviceWatcher:
  enabled: false

# Add overridable container configurations.
# Configurations specified in overrideContainers can be overridden via additional values files.
# This allows overriding image, environment variables, and resources for specific containers.
overrideContainers: {}

# # Custom configurations for HTTPRoute and HealthCheckPolicy
# gateway:
#   name: "gogateway-ai-gateway"

# routes:
#   - name: "example"
#     enabled: true
#     hostnames: # Using direct hostnames for local templating test
#       - "dev.gogateway.ai"
#       - "*.dev.gogateway.ai"
#     configMapHostnames:
#       enabled: false # Disable ConfigMap lookup for this test
#       name: "gateway-env"
#       keys: # List of keys within the ConfigMap containing comma-separated hostnames
#         - "GATEWAY_DOMAIN_NAME"
#         - "GATEWAY_WILDCARD_DOMAIN_NAME"
#       subdomains: # Optional: List of subdomains to prepend to each domain from ConfigMap
#         - "internal"  # Example: if ConfigMap has "dev.example.com", this creates "internal.dev.example.com"
#         - "api"       # Example: if ConfigMap has "dev.example.com", this creates "api.dev.example.com"
#     matches:
#       - type: PathPrefix
#         value: "/newui"
#     healthCheck:
#       checkIntervalSec: 15
#       timeoutSec: 10
#       unhealthyThreshold: 3
#       healthyThreshold: 1
#       logConfig:
#         enabled: true
#       httpHealthCheck:
#         requestPath: "/newui"
#   # Example of another route using direct hostnames
#   # - name: "another-route"
#   #   enabled: false
#   #   hostnames:
#   #     - "another.dev.gogateway.ai"
#   #   configMapHostnames:
#   #     enabled: false # Disable ConfigMap lookup for this route
#   #     name: ""
#   #     keys: []
#   #     subdomains: [] # Optional subdomain prefixes
#   #   matches:
#   #     - type: PathPrefix
#   #       value: "/api"
#   #   healthCheck:
#   #     checkIntervalSec: 30
#   #     timeoutSec: 15
#   #     unhealthyThreshold: 5
#   #     healthyThreshold: 2
#   #     logConfig:
#   #       enabled: false
#   #     httpHealthCheck:
#   #       requestPath: "/healthz"

# # Example of adding a custom service account
# #
# customServiceAccount:
#   create: true
#   name: service-watcher-sa

# # Example of adding a service watcher role binding
# #
# serviceWatcher:
#   enabled: true
#   serviceAccountName: service-watcher-sa
#   role:
#     name: service-watcher-role
#     apiGroups:
#       - ""
#       - apps
#       - batch
#     resources:
#       - services
#       - deployments
#       - pods
#   roleBinding:
#     name: service-watcher-rolebinding
